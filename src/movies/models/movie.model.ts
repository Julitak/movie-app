export class Movie {

  constructor(key: string, name: string, description?: string, genres?: string, rate?: string, length?: string, img?: string) {
  }

  id?: string;
  key: string;
  name: string;
  description?: string;
  genres?: string;
  rate?: string;
  length?: string;
  img?: string;
}
